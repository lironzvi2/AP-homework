import algorithms.mazeGenerators.Maze;

public class SimpleMazeGenerator extends AMazeGenerator {
    @Override
    public Maze generate(int rows, int lines) {
        return super.generate(rows, lines);
    }
}
